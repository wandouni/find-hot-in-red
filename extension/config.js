// 与 config.yaml crawler 配置保持一致
const CONFIG = {
  BACKEND_URL: 'http://localhost:8000',
  DELAY_MIN_MS: 6000,   // between-note browsing delay min (6s)
  DELAY_MAX_MS: 16000,  // between-note browsing delay max (16s)
  MAX_NOTES_PER_KEYWORD: 30,
  TOP_COMMENTS: 10,
};
